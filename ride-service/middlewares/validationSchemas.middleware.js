// import Joi from "joi";

// export const deliveryRideSchema = Joi.object({
//   fromAddress: Joi.string().required(),
//   toAddress: Joi.string().required(),
//   comment: Joi.string().max(200).required(),
// });
